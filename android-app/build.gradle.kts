plugins {
    id("com.android.application") apply false
    kotlin("android") apply false
    kotlin("kapt") apply false
}

// Root project for the Android build; module-specific configuration is in :app
